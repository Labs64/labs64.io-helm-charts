image:
  repository: localhost:5005/checkout
  tag: latest
  pullPolicy: Always

gateway:
  enabled: true

networkPolicy:
  enabled: true

rabbitmq:
  enabled: false

postgresql:
  enabled: false

# Cedar domain authorization (RFC-05 P4): SHADOW while the diff soaks; flip to
# ENFORCE once cedar-domain decision logs show no mismatches.
# Binding: LABS64_CEDAR_MODE -> labs64.auth.cedar.mode (application.yml).
# env:
#   - name: LABS64_CEDAR_MODE
#     value: ENFORCE
