# Copied automatically to values.secrets.local.yaml by just generate-secrets.
# The generated file is gitignored; replace these local-only defaults as needed.
secrets:
  admin:
    stringData:
      password: local-keycloak-admin
  realm-secrets:
    stringData:
      AUDITFLOW_CLIENT_SECRET: local-auditflow-secret
      PAYMENT_GATEWAY_CLIENT_SECRET: local-payment-gateway-secret
      LOCAL_TOOLING_CLIENT_SECRET: local-tooling-secret
      LOCAL_TEST_CLIENT_SECRET: local-test
      PREFLIGHT_CLIENT_SECRET: local-preflight-secret
