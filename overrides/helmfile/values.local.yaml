# Local identity-provider selection consumed by helmfile.yaml.gotmpl.
# At most one bundled provider may be enabled. Set both to false when the API
# Gateway points at an externally managed issuer.
identity:
  mockOidc:
    enabled: true
  keycloak:
    enabled: false
