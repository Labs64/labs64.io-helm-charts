image:
  repository: localhost:5005/payment-gateway
  tag: latest
  pullPolicy: Always

gateway:
  enabled: true

networkPolicy:
  enabled: true

rabbitmq:
  enabled: false

postgresql:
  enabled: false

redis:
  enabled: false

# Cedar domain authorization
env:
  - name: LABS64_CEDAR_MODE
    value: SHADOW
