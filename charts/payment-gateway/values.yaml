# -- Global values shared across Labs64.IO charts and Bitnami subcharts
# @schema
# type: object
# additionalProperties: true
# @schema
global:
  security:
    # -- Required by Bitnami subcharts when images are pulled from bitnamilegacy (image substitution guard)
    allowInsecureImages: true

# -- This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# -- This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: labs64/payment-gateway
  # -- This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # -- Overrides the image tag whose default is the chart appVersion.
  tag: ""

# -- This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# -- This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# -- This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # -- Specifies whether a service account should be created
  create: true
  # -- Automatically mount a ServiceAccount's API credentials?
  automount: true
  # -- Annotations to add to the service account
  annotations: {}
  # -- The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# RBAC configuration (create RBAC resources: Role, RoleBinding)
rbac:
  create: false
  rules: []
    #- apiGroups: [""]
    #  resources: ["pods"]
    #  verbs: ["get", "list", "watch"]

# -- This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# -- This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# -- This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # -- This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # -- This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 8080

# -- This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: "nginx"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: localhost
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

# -- IngressRoute configuration for Traefik more information can be found here: https://doc.traefik.io/traefik/routing/providers/kubernetes-crd/
ingressroute:
  # -- This sets whether the IngressRoute is enabled or not
  enabled: false
  # -- Host for the IngressRoute
  host: localhost
  # -- Entry points for the IngressRoute
  entryPoints:
    - web
    - websecure

resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 100m
    memory: 512Mi

# -- This is to setup the liveness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /actuator/health/liveness
    port: 8080
  initialDelaySeconds: 30
  periodSeconds: 10
  failureThreshold: 3
  timeoutSeconds: 2

# -- This is to setup the readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
readinessProbe:
  httpGet:
    path: /actuator/health/readiness
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 3
  timeoutSeconds: 2

# -- This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# -- Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# -- Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

env: []

# -- Secret data to be used as environment variables (delivered via envFrom).
# External installs supply broker/database/cache credentials here, e.g.
#   SPRING_RABBITMQ_USERNAME / SPRING_RABBITMQ_PASSWORD, SPRING_DATASOURCE_USERNAME / SPRING_DATASOURCE_PASSWORD, SPRING_DATA_REDIS_PASSWORD.
# When rabbitmq.enabled=true / postgresql.enabled=true / redis.enabled=true the chart adds these keys automatically from rabbitmq.auth / postgresql.auth / redis.auth.
# Keys you set here take precedence over the bundled-dep keys. On helm upgrade the Secret is deleted and recreated (hook-managed).
# Note: the Secret is hook-managed (pre-install) and survives helm uninstall.
# @schema
# type: object
# properties:
#   data:
#     type: object
#     additionalProperties: true
# @schema
secrets:
  data: {}

# -- Optional bundled RabbitMQ (Bitnami subchart) for standalone/local installs.
# Dev-grade credentials - NOT for production; point applicationYaml at your own broker instead.
# @schema
# type: object
# additionalProperties: true
# @schema
rabbitmq:
  enabled: false
  # -- docker.io/bitnami versioned tags were moved to bitnamilegacy; keep in sync with the subchart's default tag
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  image:
    repository: bitnamilegacy/rabbitmq
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  auth:
    username: labs64
    password: labs64pw

# -- Optional bundled PostgreSQL (Bitnami subchart) for standalone/local installs.
# Dev-grade credentials - NOT for production; point applicationYaml at your own database instead.
# @schema
# type: object
# additionalProperties: true
# @schema
postgresql:
  enabled: false
  # -- docker.io/bitnami versioned tags were moved to bitnamilegacy; keep in sync with the subchart's default tag
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  image:
    repository: bitnamilegacy/postgresql
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  auth:
    username: labs64
    password: labs64pw
    database: payment_gateway

# -- Optional bundled Redis (Bitnami subchart) for standalone/local installs.
# Dev-grade credentials - NOT for production; point applicationYaml at your own Redis instead.
# @schema
# type: object
# additionalProperties: true
# @schema
redis:
  enabled: false
  # -- docker.io/bitnami versioned tags were moved to bitnamilegacy; keep in sync with the subchart's default tag
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  image:
    repository: bitnamilegacy/redis
  architecture: standalone
  # @schema
  # type: object
  # additionalProperties: true
  # @schema
  auth:
    password: labs64pw

# -- Additional application properties
applicationYaml:
  # -- Spring configuration
  spring:
    # -- RabbitMQ connection params
    rabbitmq:
      # -- RabbitMQ host; resolves to the bundled subchart service when rabbitmq.enabled, else set your broker host
      host: '{{ ternary (printf "%s-rabbitmq" .Release.Name) "rabbitmq.tools.svc.cluster.local" .Values.rabbitmq.enabled }}'
      # -- RabbitMQ port; default: 5672
      port: 5672

    # -- PostgreSQL connection params
    datasource:
      # -- JDBC URL; resolves to the bundled subchart when postgresql.enabled, else set your database URL
      url: '{{ ternary (printf "jdbc:postgresql://%s-postgresql.%s.svc.cluster.local:5432/payment_gateway" .Release.Name .Release.Namespace) "jdbc:postgresql://postgresql.tools.svc.cluster.local:5432/payment_gateway" .Values.postgresql.enabled }}'

    # -- Redis connection params
    data:
      redis:
        # -- Redis host; resolves to the bundled subchart when redis.enabled, else set your Redis host
        host: '{{ ternary (printf "%s-redis-master" .Release.Name) "redis-master.tools.svc.cluster.local" .Values.redis.enabled }}'
        # -- Redis port; default: 6379
        port: 6379


# -- Gateway routes published by this module (rendered by chart-libs.gateway-routes)
gateway:
  # -- Publish this module's routes on the Traefik gateway
  enabled: false
  # -- External path prefix; defaults to /<chart-name>
  prefix: ""
  # -- Traefik entry points
  entryPoints:
    - web
    - websecure
  # -- Names of the shared middlewares provided by the gateway-common chart
  sharedMiddlewares:
    auth: gateway-common-auth
    rateLimit: gateway-common-ratelimit
    securityHeaders: gateway-common-security-headers
  # -- Routes exposed by this module
  routes:
    # -- Payment Gateway API (protected, prefix stripped before forwarding)
    - path: /api
      port: 8080
      stripPrefix: true
      roles:
        - admin-role
        - ecommerce-role
        - default-roles-labs64io
    # -- OpenAPI docs (public, prefix stripped before forwarding)
    - path: /v3/api-docs
      port: 8080
      public: true
      stripPrefix: true
# -- PodDisruptionBudget (rendered by chart-libs.pdb)
podDisruptionBudget:
  enabled: false
  minAvailable: 1

# -- NetworkPolicy: allow ingress from Traefik and same-namespace pods only (rendered by chart-libs.networkpolicy)
networkPolicy:
  enabled: false
  # -- Namespace where Traefik runs
  gatewayNamespace: tools
  # -- Additional raw ingress rules
  extraIngress: []

# -- Values passed to the chart-libs library dependency (present so the generated schema accepts the key Helm injects for the dependency)
# @schema
# type: object
# additionalProperties: true
# @schema
chart-libs: {}

# -- helm test hook (rendered by chart-libs.test-connection)
tests:
  enabled: true
  # -- Health endpoint probed by `helm test`
  healthPath: /actuator/health
