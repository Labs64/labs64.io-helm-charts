{{- if and .Values.ingressroute.enabled .Values.ingressroute.middlewareSpec }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "chart-libs.fullname" . }}-{{ .Values.ingressroute.middlewareSpec.name }}
  labels:
    {{- include "chart-libs.labels" . | nindent 4 }}
spec:
{{ toYaml .Values.ingressroute.middlewareSpec.spec | nindent 2 }}
{{- end }}
