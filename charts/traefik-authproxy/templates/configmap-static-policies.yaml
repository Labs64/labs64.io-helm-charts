apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart-libs.fullname" . }}-static-policies
  labels:
    {{- include "chart-libs.labels" . | nindent 4 }}
data:
  static_policies.yaml: |-
{{ toYaml (dict "policies" (.Values.staticPolicies | default list)) | indent 4 }}
