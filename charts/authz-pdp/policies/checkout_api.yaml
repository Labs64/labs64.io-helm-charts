# GENERATED from x-labs64-auth by OpenApiAuthPreprocessor — do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: checkout_api
  schemas:
    principalSchema:
      ref: cerbos:///principal.json
  rules:
  - actions:
    - listCustomers
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"customer:read\")"
  - actions:
    - createCustomer
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"customer:write\")"
  - actions:
    - getCustomer
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"customer:read\")"
  - actions:
    - updateCustomer
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"customer:write\")"
  - actions:
    - createPurchaseOrder
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"purchase-order:write\")"
  - actions:
    - listPurchaseOrders
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"purchase-order:read\")"
  - actions:
    - getPurchaseOrder
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"purchase-order:read\")"
  - actions:
    - updatePurchaseOrder
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"purchase-order:write\")"
  - actions:
    - checkoutPurchaseOrder
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"purchase-order:checkout\")"
  - actions:
    - listCheckoutTransactions
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"checkout-transaction:read\")"
  - actions:
    - getCheckoutTransaction
    effect: EFFECT_ALLOW
    roles:
    - '*'
    condition:
      match:
        expr: "(has(request.principal.attr.tenant) || (\"service\" in request.principal.roles)) && request.principal.attr.scopes.exists(s, s == \"checkout-transaction:read\")"
