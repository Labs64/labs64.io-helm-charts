apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart-libs.fullname" . }}-policies
  labels:
    {{- include "chart-libs.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "policies/*.yaml").AsConfig | indent 2 }}
